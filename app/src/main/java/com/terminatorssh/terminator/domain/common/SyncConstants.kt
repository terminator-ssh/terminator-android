package com.terminatorssh.terminator.domain.common

import kotlin.time.Duration.Companion.seconds

object SyncConstants {
    val AUTO_SYNC_DELAY = 3.seconds
    val DEFAULT_API_URL = "https://terminator.sh/api/v1"
}